# Project Memory

# DSH Project Context — Project Memory

## Project
- Root `/mnt/Data/Projects/dsh-project-context`; four dsh plugins: `project-context` (archives session log + `session.md` + `INDEX.md`, no LLM), `project-memory` (logs + memory/context → `CONTEXT.md`/`MEMORY.md`), `project-autolearn` (CONTEXT/MEMORY + archived sessions → `.agents/skills/<name>/SKILL.md`), `project-handoff` (hands a session to a successor near the context limit).
- Installed as a `link:` dependency in **both** `~/.dsh/profiles/web/package.json` and `~/.dsh/profiles/desktop/package.json`; host-side changes need that host restarted, client changes are live after `pnpm build` (served per request, so only a tab hard-reload). The host actually serving this GUI is the **desktop** app — see "Which host serves this GUI".
- Remote `ssh://ssh.github.com:443/P02-1010751281/dsh-project-context.git`, branch `main`; commits are pushed as established practice.
- Toolchain: pnpm, `tsc`, `node --test`. Node lives in the Nix store and its path rotates on every rebuild — verify with `node -v`, never trust a recorded path. Nix node 22.23.2 at `/nix/store/p8wnn48gi30j0n0a6ispjinnb6ykz28i-nodejs-22.23.2/bin`; system node 24.20.0.
- Normal untracked entries: `.agents/memory/session-logs/`, `.agents/skills/`. **Committed 2026-09-26 at the user's request**: `.agents/memory/MEMORY.md` + `CONTEXT.md` (durable memory) and `.agents/evidence/2026-09-26-settings-card-render/` (render evidence). `docs/upstream-pi-triage.md` was already tracked (`01809c5`, 2026-09-25).

## Verification state
- `748e3f2` (docs/evidence/memory only — no `src/` change): typecheck 0, build 0, **225/225** tests; `lib/` mutant markers 0, `lib/client.js` 28380 B. Re-run 2026-09-26 on this exact revision, so the **code** baseline is still `b2c9402`.
- `b2c9402`: typecheck 0, build 0, **225/225** tests; `lib/` mutant markers 0. Client `lib/client.js` 28380 B.
- `4896cfd`: typecheck 0, build 0, **206/206** tests; `lib/` mutant markers 0.
- `a7b1c1d`: typecheck 0, build 0, **195/195** tests (194 at `e101db1`).
- 设置卡片**真机渲染已证实**（2026-09-26）：证据在 `.agents/evidence/2026-09-26-settings-card-render/`（截图 + 判据 + 边界）。15:32:35/39 的帧显示插件页 `dsh-project-context` v0.2.0：布尔=平台开关、三组枚举=胶囊（`off|session` / `defer|wait` / `auto|zh|en`）、底部**只有**平台「保存」、无「放弃」。结构断言由 `test/card-render.test.mjs` 用 `react-dom/server` 钉住（21 行 = 8 卡片自绘 + 13 平台自绘、5 开关、7 胶囊、保存 = 一次带 revision fence 的原子写入）。「服务端跑的是迁移后卡片」的判据：`lib/client.js` 内 1 处平台 require，而被删的手抄卡片 0 处。
- 判据陷阱：**帧的文件大小不是页面分类器**——「插件页尺寸」那批大帧其实是会话页。卡片帧反而更小。
- `test/card-render.test.mjs` 要自己供 `react-dom`/`clsx`/`zustand`/`immer`（devDependencies），因为 `dsh-client-store` 与 `dsh-client-ui-primitives` 的 `lib/index.js` import 了它们、manifest 却只声明 cordis peer；`ui-primitives` 的单一 mega-index 还会 import shiki/katex/micromark/simple-icons，测试对这批注入惰性 passthrough 并在 `t.diagnostic` 列出（浏览器 bundle 不受影响，它把这些全 external）。
- `node --test` prints TAP (`# pass 195`) when stdout is not a TTY — check the exit code, not a grep for an interactive reporter. `pnpm test` is `tsc && node --test`; `node --test test/` (a bare directory) fails, so run the package script.

## Upstream ground truth
- `git clone` works from this host even though `web_fetch` on github.com / raw.githubusercontent.com / stackoverflow.com does not (only search snippets arrive). Use `git clone --depth 1 --filter=blob:none --sparse <url> /tmp/<name>` + `git sparse-checkout set <paths>`; delete the copy when done.
- Rule: any claim about an external API/field/formula → clone and read source, never infer from the local code path or blog prose.
- Rule: before reasoning about a local repo, `git fetch` and compare `HEAD` with `origin/<branch>`; state the revision read; an empty grep is evidence of that revision only.
- pi checkout `/mnt/Data/Projects/pi-project-context`; `HEAD` = `origin/master` = `ed2704c`. pi's tests DO run here: the package directory is named `pi-monorepo` (so `findPiPackage()` misses it); run `PI_PKG=<explicit path> node tests/run-all.mjs` → 9/9. Pass **one** path — the nix glob can match several pi versions and `$(ls -d ...)` concatenates them into a broken path, producing a false `9 of 9 failed` + `ERR_MODULE_NOT_FOUND`. pi has **no** subagent concept (`grep -rl subagent dist` = 0), and its `newSession()` does `abort()` → `dispose()` on the outgoing session, so a handoff never needs a subagent guard there.
- **dsh subagent listing: `activity` is RESIDENCY, not turn activity.** `list-children.ts` builds rows as `childRow(childId, identity, 'running', …)` for every candidate `sessions.get(id)` still holds and `'inactive'` only for a cold read (`cache`/`observeSession`). So "a resident child between messages" reads as `running`. Upstream's own `list_agents` tool re-derives status from the live registry — `agents.get(id)?.status === 'running' ? 'running' : 'inactive'` — with the comment "Report turn activity without exposing whether the child is loaded"; the Team roster (`availability`) and `archive-admission` read `agent.status` too, and `AgentStatus` is `'idle' | 'running'` (dispatched as the `agent/status` event, `agent-loop/src/agent.ts:149`). **Never read the listing's `activity` as "is working now".**
- `subagents.listChildren(parentId)` returns direct children from the parent-owned `subagent/catalog` projection: `SubagentListEntry` (`{kind, id, mode, activity, hasChildren}`) in **0.1.6**, `SubagentCatalogEntry` (`{id, createdAt, mode, label}`) from **0.1.7-alpha.1** on. Both carry `mode`, so one read covers both versions; the classified `kind`/`activity` rows moved to `listDescendants` (whole tree). Teammates are in this catalog: `roster.spawn` → `subagents.startContinuable` → `continuation.ts:184` `establishCatalogChild(parent.session, childHeader, {mode:'continuable', label})`.
- A plugin reads undeclared services with `ctx.get(name)` — cordis documents it as "Read a service from the store **without the inject requirement**" (`strict` only filters fibers that are not active); it returns `undefined`, never throws. `ctx.foo` (the property proxy) *is* caller-scope bound and does throw without injection.

## Release / versioning
- `v0.1.0` is the first release: an annotated tag on `5f96596`, pushed. `package.json` is 0.1.0. There are **no** GitHub Releases — do not re-raise.
- The changelog lives in `CHANGELOG.md` (not the README), listed in `package.json` `files`; post-tag changes go under `未发布`. README prose wraps at 100 columns; never split an inline code span across lines.
- Commit messages are English Conventional Commits.

## Commits (main)
- **Last `src/` commit (the code baseline)** = **`b2c9402`** (`style(handoff): drop the comments that still name the deleted throttle`, 2026-09-26; before it `c8c8d79` headless card render test, `4896cfd` subagent-activity fix). `origin/main` read **`748e3f2`**, unpushed 0, on 2026-09-26 16:04 — but a recorded HEAD is a *read-at-revision* value, not a fact: `748e3f2` is the very commit that carries this memory, so it is stale the moment anything else lands, and it was already shipped stale once (this line previously said `b2c9402` while `748e3f2` was HEAD). **Re-derive with `git rev-parse HEAD origin/main`; do not copy this line.** The 2026-09-25 subagent-guard chain: `db701fc` (guard introduced, read `activity` — wrong from day one) → `c6f7988` (row-shape drift) → `d0be028` (manual `/handoff now` refuses and names the child) → `4896cfd` (predicate = live registry). Earlier this session: `a7b1c1d` (knee fallback chain + resolver split) ← `011a68e` (revert of the wrong cap) ← `8557fb1` ← `e101db1`.
- Parent chain: `e101db1` (report the real failure cause) ← `50d5f06` (optional-inject client repair, pushed externally) ← `0d3574e` ← `f2acbae` ← `3da2517` ← `01055ea` ← `92402f9` ← `fdc5722` ← `a6510a3`. The v0.1.0 tag points at `5f96596`, not HEAD; read `git log` for the authoritative chain.
- External repair context — **a locally maintained patch set, not upstream** (it exists because dsh 0.1.7-alpha.1 removed the client-side `settingsScope` service). As of 2026-09-26 `dsh-client-auto-continue` is at **0.11.8, not 0.11.7**: the client half was fixed upstream in 0.11.8 (it moved to `ctx.configForms`), so the three client-side patches were deleted; the patch now only adds the host `typeof`/`try-catch` guard plus the per-field `volatile` `Config` export that makes the settings card appear at all. It is declared in each profile's **`pnpm-workspace.yaml` `patchedDependencies:`** (not `package.json`), keyed to the exact version, and pinned in Nix at `/etc/nixos/home-manager/user/programs/dsh.nix:25`. Archive `/etc/nixos/scripts/dsh-plugin-patches/` (README, `@0.11.8.patch`, `PINNING-RISK.md`; tracked in /etc/nixos). **Residual risk**: the profile *dependency* is `^0.11.8` (a range) while the patch key is exact — one manual `pnpm add`/`pnpm update` in a profile drifts to 0.11.9, pnpm then **silently skips** the patch, and the desktop crashes at the next boot (`web boot: N entries did not activate`). `scripts/check-dsh-plugin-patches.sh` (green for **both** profiles 2026-09-26, wired into `scripts/check.sh`) is the only automated catch, and it does not run at desktop launch. Profiles are runtime state (not Nix, not git) → a rebuild must re-apply from the archive. `minimumReleaseAge: 0` is deliberate (pnpm 11's 24 h wait rejects the newest release); alpha.1-era residual — the card not rendering — is moot on rc.2.
- **Which host serves this GUI (checked 2026-09-26): the desktop app, not `dsh web`.** `127.0.0.1:19387` is the electron desktop (PID 82600, started 15:36:56, host child node PID 82709 running `~/.dsh/desktop/nix-project/…`); **nothing listens on 3080** and there is no `dsh web` process. Both profiles link this repo. A host-side change is live iff the host start is later than the build carrying it: this host started 2026-09-26 15:36:57, and the only later `src/` commit (`b2c9402`) is comment-only → the live host is **functionally current and no restart is pending**; the standing 「restart `dsh web` to load a7b1c1d/e101db1/…」 task was already satisfied by that launch and is misdirected anyway. Client-bundle changes never need a host restart (served per request, rev-hashed) — only a tab hard-reload. Restarting the desktop is **not** 「click the icon again」: the single-instance lock keeps the old host on `:19387`, so it must be fully quit first.

## Upstream pi port batch (CLOSED)
- Ported: P3 `27f7043` → `fdc5722`; P3b → `3da2517`; P4 `09ea0c7` → `92402f9`; P2 `0af9ec6` → `f2acbae`; post-batch `0d3574e`; knee P1 `3ee5794` reinterpreted → `8557fb1` (cap, wrong) → `011a68e` (revert) → `a7b1c1d` (fallback chain).
- Not ported: `d4cdad3` (2)(3)(4), `8a2e6e4`, `af93fab`, `093dbf3` NOT-APPLICABLE. Triage archived at `docs/upstream-pi-triage.md` — **already committed in `01809c5`** (2026-09-25; it declares itself CLOSED and is a record, not a plan; `docs/upstream-issue-client-send-path.md` remains the home for the open upstream-issue analysis). Any memory entry calling it untracked is stale.
- Transfer trap: dsh's finish reason is `max-tokens`, pi's is `length`; copying pi's string silently disables the retry path (mutant-verified: 3 tests red).
- A new config key costs: host field + settings schema + card spec + projection + rendered row + two locales; `settings-form.test.mjs` scans every schema key, so skipping the `client/` edits fails it.

## Memory cap and the loaded-memory trap
- `MAX_MEMORY_CHARS` 32000 (was 24000), `MIN_MEMORY_CHARS` 4000, limit 200000, configurable via `maxMemoryChars`. All 9 `normalizeMemoryDocument` call sites take the limit.
- A capped document is cut on a line boundary (`clipToLineBoundary`, never a bare slice) and gets a marker `_[memory truncated at <limit> characters: <dropped> dropped]_`. Since the 2026-09-25 split: `isMemoryTruncated(text: string)` and `clipToLineBoundary` live in `src/project-memory/document.ts` (**takes the document string, not the `LoadedMemory` object**); `loadMemory(projectRoot, limit)` is **async** and returns `{text, source, poisoned, unreadable?, damaged?}` from `src/project-memory/load.ts`.
- The real failure this fixed was silent permanent loss of newly appended content: new lines land past the cut and are dropped on write (both journal append `:709` and render write `:712` go through `normalizeMemoryDocument`).
- Trap: `loadMemory` returns a document already carrying the marker while the status object reports `damaged: 0`, `poisoned: false`, so `/memory status` showed a healthy document with a third of the content gone (measured: 31888 chars, 9621 dropped). P3b (`3da2517`) now reports the cap state.
- Editing `MEMORY.md` with the edit tool bypasses the plugin write path and can leave the file above the cap; the next consolidation silently drops the excess. Check `wc -c` plus `loadMemory(root, 32000)` and `isMemoryTruncated(loaded.text) === false`; budget ~31.5K. Last measured 2026-09-26 (after the stale-HEAD correction): **~27.9K loaded**, `truncated: false`, `damaged: 0`, `poisoned: false`（余量 ~4.1K）— record the round number, not a brittle exact count, and re-derive it from the API each time.

## Failure-cause attribution (R1/R2/R3 audits; both R3 items CLOSED 2026-09-26)
- The central defect class in both repos is **misattribution**: distinct failures collapse into one `undefined` and the caller reports an actively wrong cause. Silent degradation is tolerable; a wrong cause is not.
- dsh's three instances are FIXED in `e101db1` (receipt layer only; `resolveThreshold`'s formula and two-exit shape untouched): `/handoff status` used to render every `undefined` as a claim about the window (counterexample: W=40000, keep=0, baseline=11800 → usable 23616 > floor 19800 yet refusal, because `min(19800, usable−4096) = 19520 < floor` — the 4K `SAFETY_MARGIN_TOKENS` applied a second time); `/handoff now` now splits deferred / transient / terminal; `/autolearn` now reports skipped / rejected instead of claiming the model proposed nothing.
- `handoffFailureIsTransient` took ~9 adversarial rounds; adjacent rounds swapped the same bug pair. Convergence came from a **discriminator** (a value-noun guard over a bounded prefix window), not a better regex; the final confirmation round added no new errors in either direction across two inherited matrices. Both previously SURVIVED mutants were killed.
- Residual misclassifications are enumerable and documented as known accepted in source comments + CHANGELOG (double negation, non-English value nouns, a trailing value after `model`/`port`/`line`, underscore/plural overload identifiers, finite best-effort word lists). The `overload` actor branch is the fragile corner (config statements such as `service overload protection enabled`; a leading boundary does not fix it because the actor sits at the sentence head). Do not present these as fixed.
- pi's refusal receipt is FIXED (`6e620a5`): `resolveThreshold` (now `extensions/project-context/handoff/threshold.ts:109`, post-split `18af40b`) has six `undefined` exits (one is billing — `tierEdge - TIER_EDGE_MARGIN < floor`); `thresholdRefusal` / `thresholdRefusalText` name the cause and `statusText` is exported. Reverting the rendering fails 3 checks; the existing suite pinned the refusals but never what the user was told.
- Both R3 items are **CLOSED** (re-verified 2026-09-26): `06c6059` deleted the 15 s pressure-check throttle outright — `pressureCheckedAt` no longer exists anywhere in `src/`, every `turn/end` re-measures — and `2745df4` made `approveCandidate` share the exported `shapeRejection` predicate with `rejectionReason` (`src/project-autolearn/candidate.ts:47`, pinned by `test/autolearn.test.mjs:624` from three angles: predicate, pass, approve). Every pre-split reference (`src/shared/autolearn.ts:275`, `src/handoff.ts:…`, bare `:NNN` numbers) is a **stale path** from before the one-module-per-responsibility split (`843c2ed`/`0493dcf`) — the code now lives under `src/project-*/`; cite symbols, not old line numbers.
- **Do NOT split `performHandoff`** (`src/project-handoff/perform.ts:73`, 114 lines) — every step delegates to a named helper. **`maybeAutoHandoff` IS genuinely mixed** and is the highest-risk refactor here (it guards the 2026-09-17 double-write incident).
- Duplicate copies of rules are the recurring root cause: `approveCandidate` vs `rejectionReason` (pi 2 vs 8 rules); the mtime-wins rule written three times — `renderInfo.mtimeMs > journalInfo.mtimeMs` in `src/project-memory/load.ts:74` and `record.ts:46`, plus `load.ts:156` comparing `mtimeMs(target) > mtimeMs(journal)` with no stat guard, surviving only because the helper returns 0 for a missing file; `notify` redefined. The fix is one shared pure function, not a third copy.
- **CLOSED 2026-09-26**: pi's `/autolearn approve` used to bypass the body cap and injection detector (a 25,001-char body with an injection phrase activated intact). Both repos now share one predicate — dsh `src/project-autolearn/candidate.ts:47` `shapeRejection`, pi `extensions/project-context/autolearn/candidate.ts:34` (`:88` calls it from `approveCandidate`), landed pi-side by `33e8d9e`/`1aff2aa`. Nothing upstream left to file.
- The regression guard proposed for `50d5f06` **landed**: `test/client-inject.test.mjs:34-68` pins the static `inject` list to exactly `["slots","locale"]`, requires neither `settingsScope` nor `configForms` to be listed, and separately asserts the card still requests `configForms` through an optional `ctx.inject` (so the first assertion cannot pass by deleting the feature).
- Coverage corrections (audited pre-split — re-verify ownership before acting): `runHandoff` is error-uncovered; `statusText` had none; `rejectionReason` zero direct coverage; pi's `resolveThreshold` guard `!model || usage.tokens === null` (`extensions/project-context/handoff/threshold.ts:122`) none; pi's truncation-retry guard `/token cap|incomplete/i` (`handoff/summary.ts:65`) none, and it decides retry by matching **upstream error prose** — the `Error` thrown by `shared/llm.ts:98-107` carries no `stopReason` field, so any rewrite must thread that through first.

## Threshold design — dsh vs pi
- pi's `kneeTokens` is a cross-model quality curve fitted to MRCR 8-needle population data: `round(W − (W − 157K) / (1 + e^(−ln(W/450K)/0.04)))`, K = 157K (the asymptote, p50 of 46 models ≥ 1M; p25 127K, p75 190K), Wc = 450K, s = 0.04. Purpose: distrust declared windows (grok-4.5/4.6 declare 500K with a ~195K family knee; DeepSeek-V4.1-Flash / GLM-5.3 / Qwen3.8 declare 1M with measured knees 130–170K). At ≤ ≈400K `knee(W)` exceeds `usable − SAFETY_MARGIN`, so capacity binds, not the curve.
- **dsh knee port CORRECTED in `a7b1c1d`** (after `8557fb1` was reverted by `011a68e`). The user's framing 「上游优先 + knee 回退保底」 means a **回退链**, NOT a cap: 「两层**不是加起来**，优先上游字段，没有就根据 knee 推算」, 「**tokens = auto_compact_token_limit ? auto_compact_token_limit : knee(contextWindow)**」. Correct shape: `quality = upstreamUsableInput ?? knee(window)`; then pi's order — `threshold = min(max(baseline+keep+summarizeAmount, quality), usable − SAFETY_MARGIN)`. Quality is the base, the configured target **raises** it (so `handoffTargetTokens` stays meaningful), capacity has the last word. **`8557fb1` had it as `min(configured, knee)` — a cap that lowers an explicit target — which is the opposite, and it made an absent upstream field mean the curve does nothing.**
- **This is a behaviour change, not a no-op**: default `handoffTargetTokens` 64000 with baseline 6000 / keep 20000 gives 65K 37576→**45152**, 128K 68808→**107616**, 200K 90000→**179616**, 400K 90000→**379616**, 1M/2M 90000→**157000** — exactly pi's documented `boundary` shares 84% / 90% / 95% / 16% / 8%. The old 90K was `baseline + keep + target` with no quality layer at all. `W=40000 / keep=0 / baseline=11800` still refuses (capacity 19616 < floor 19800), so the misattribution fix's test survives.
- **dsh exposes NO upstream usable-input field** (`LlmResolvedModelInfo.context` is only `{ contextWindow: number }`; no `auto_compact_token_limit` / `usableInputTokens` / `maxInputTokens` anywhere in dsh). `qualityLimit(window, upstream?)` is the seam for the harness change that splits declared capacity from usable input — pass nothing until then; do not fake the first branch. `discovery.ts` (`capacity()` first-match-wins over five spellings) is dsh **core**, not this repo.
- **`resolveThreshold` was split** (`a7b1c1d`) into `handoffRoom` (feasibility only), `qualityLimit` (the chain), `capacityLimit` (the bound), `summarizeAmount` (how much to fold) + an orchestrator that only selects a mode and composes them. It used to mix six jobs, which is why the refusal text and the formula each re-derived the same terms and could drift; `thresholdRefusal` now reuses `handoffRoom`. The user asked for this twice («另外还有没有一个函数混了多个任务的问题？»).
- Mutation-verified on the chain: reversing it fails 1 check, defeating the capacity cap fails 11, making the target inert fails 1. **The first two attempts at the cap/target mutants failed `tsc` (`TS6133`, symbols left unused) — an invalid mutant's red is not evidence; reshape so every symbol stays referenced.**
- **Lesson: my own memory had the knee direction wrong and I trusted it over the sessions.** The user had corrected this several times in `session-8256f99d`; the corrections were recoverable only by parsing `user/message` events with `source.kind == "user"` out of `.agents/memory/session-logs/*/session.jsonl` (runtime-context snapshots and handoff banners are also `user/message`, so filter them out or they drown the signal). When a user says 「我矫正过好几遍」, read the session logs before defending the code.
- pi's `resolveThreshold` is overloaded — five jobs in one function (mode selection; feasibility, where `undefined` means "can we hand off at all"; the physical threshold; a cross-route summarizer cap; billing). Do not add a quality ceiling into it without splitting into `canHandoff` / `summarizeCapacity` / `costTierCap` / `qualityLimit`.
- Fence decision (agreed, applies to dsh and pi): a quality ceiling that overrides an explicit user setting must not be silent. (A) apply it in auto mode only and warn but do not clamp in non-auto; or (B) apply uniformly and emit a notice. pi is design (A) by construction but does not warn; dsh now warns via the receipt.
- Retracted analyses — do not reuse: "90K is insensitive to window size, therefore a bug" (the constant is deliberate — large windows stay shallow on purpose, small windows go near the feasible boundary on purpose); `targetOlder = min(handoffTargetTokens, conversationRoom/2, window × SHARE)`; a self-inconsistent cost table; making `keep`/`MIN_SUMMARIZE_TOKENS` window-scaled.
- Cost at equal task size: pi/dsh ≈ 88500/55000 ≈ 1.61×; at 400K pi's 95% trigger replays ≈ 199,808 tok per turn vs dsh's ≈ 55,000 (3.63×); prompt caching widens dsh's advantage.

## Codex context-window semantics (source-verified)
- Source: `codex-rs/protocol/src/openai_models.rs` and `codex-rs/core/src/session/context_window.rs`.
- Fields: `context_window` (declared), `max_context_window` (cap for config overrides), `auto_compact_token_limit`, `effective_context_window_percent` (default 95). User config key is `model_context_window`. `max_input_tokens` is NOT an OpenAI/Codex field — it is an OpenRouter-family gateway spelling that dsh's `discovery.ts` probes among five.
- Arithmetic: `resolved_W = context_window.or(max_context_window)`; `full_context_window_limit = resolved_W × effective_percent / 100`; `auto_compact_token_limit() = min(config_limit, resolved_W × 9/10)`. Shape to copy: an `or` chain, then a same-base `min`; the hard-capacity limit is computed independently from the same W and always applies.
- Codex's own known bug is a base mismatch (the threshold uses the original window while "usable" is 95%). And `model_auto_compact_token_limit` can only be lowered — a raise is silently ignored.
- dsh `discovery.ts:91` `capacity(...)` is first-match-wins over five spellings; `LlmDiscoveredModel` carries only `contextWindow`/`maxTokens`, so finer metadata cannot reach a plugin — upstream fix, not this repo.

## Probe traps
- pi harness probes must pass `cwd`: `makePi({ cwd: tmp })`, else the exec stub returns `process.cwd()` and `getProjectRoot` searches the wrong directory (a false "No candidate named …").
- dsh's skill-log directory is `.agents/memory/session-logs` (`project-state.ts:111`); fixtures elsewhere make `archivedSessionIds` empty, so every case is rejected by the evidence rule — a fabricated "all cases behave the same".
- `tsc` emits tagged templates as `String.raw` followed by the template **with a space**, so `grep -cF` for an in-template marker returns 0 and looks like a failed restore or an empty mutant; locate mutants with `grep -n "<NAME> = "` in `lib/`.
- Counts of "reachable states" are domain-dependent (24 / 84 / 170 / 12 / 1608 were all reproduced under different scan domains) — never cite a bare count; give the mechanism plus a named domain.
- A report whose metadata is wrong must not be cited as-is: re-verify a finding's path/line before acting on it.
- Probes in `/tmp` resolve relative imports against themselves — import host modules by absolute path.

## Conventions
- NEVER hand off, compact, or end a turn while a teammate is running — in-flight teammates die with the parent turn and their work is lost. They are in-process (no entry in `~/.dsh/sessions/`) and show only `provisioning → active`, never a terminal phase, so a dead teammate leaves no failure event. Rules: `list_agents` and wait for every running teammate (or interrupt it explicitly) before any handoff/compaction; `send_message` to an inactive member is untrusted; confirm by re-checking `list_agents`/`team_task_list`, never by the send's accepted status; recover partial artifacts from `/tmp` instead of blindly re-spawning; require every teammate to write its report to a fixed path as it goes.
- A teammate that dies mid-mutation leaves `src/` dirty: snapshot `src/` (copy + `sha256sum`) into the task text before spawning, require the restore command in the report, and run `sha256sum -c` after every teammate settles or dies; treat an unrestored mutation as unverified work.
- An unvalidated mutant produces an invalid red. A valid mutant must (a) compile with 0 errors, (b) have its marker in `lib/` **after** `pnpm build`, (c) demonstrably change behaviour on a probe input. Restoring `src/` does NOT restore `lib/` — end a mutation round with a rebuild and marker count 0. Edit `src/` (editing `lib/*.js` is overwritten and yields a false SURVIVED), restore from a hash-verified `/tmp` copy, never via `git checkout --` or `git stash`, and scope the run at least to the file that owns the pinning test.
- A heuristic wording classifier does not converge by patching — it trades one error class for another. When two rounds swap the same bug back and forth, you are missing a discriminator, not a pattern.
- Memory stays concise (< 6000 words); no secrets, credentials, or generic advice; unresolved work lives in memory/context, not in skills.
- Do not touch the user's own dsh sessions; goal/session control belongs to the user's agent session. Read-only investigation of `~/.dsh` stores is fine (`zstdcat session.v3.jsonl.zstd`); throwaway probe sessions only under `/tmp`, archived afterwards.
- A second agent session may be working in this repo concurrently: stage only the files changed for the current task (`git add <paths>`, never `add -A`) and commit with `git commit -F <msg> -- <paths>` (options before `--`).
- Do not restart the user's `dsh web` service unprompted; propose the restart.
- Fixes must be verified by probe/API check before implementation, not adopted from review prose. Each batch gets an independent adversarial review, and new tests are mutation-checked against their fix.
