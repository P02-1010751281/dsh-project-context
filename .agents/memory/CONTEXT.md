# Project Context

Last updated: 2026-09-26T07:57:00.000Z

## Summary

Corrected the auto-handoff threshold to pi's **two-layer mechanism**. The first attempt (`8557fb1`) read 「上游优先 + knee 回退保底」 as a **cap** — `min(configured, knee)` — which was backwards: with the knee as a cap, an absent upstream field means the curve does nothing, whereas the design is a **fallback chain** (`quality = upstreamUsableInput ?? knee(window)`). Reverted it (`011a68e`) and re-landed the correct shape (`a7b1c1d`): quality is the base, the configured target raises it (so `handoffTargetTokens` stays meaningful), and the capacity cap has the last word. It **is a behaviour change** (the old 90K trigger becomes ~90–95% of the window below ~700K, and the 157K asymptote above it). Simultaneously split `resolveThreshold` — which mixed six jobs — into `handoffRoom` / `qualityLimit` / `capacityLimit` / `summarizeAmount` plus a composing orchestrator, the item the user had asked for twice. Gate 0/0/195; three valid mutants killed. **Those host-side changes are no longer inert**: the desktop host serving this GUI started 2026-09-26 15:36:57, later than every functional `src/` commit (the only later one, `b2c9402`, is comment-only) — see the corrected restart entry under Open tasks.

## Key points

- `origin/main` = **`b2c9402`**（2026-09-26）。下面这段 knee 批次是 `a7b1c1d`：`feat(handoff): apply the quality knee as a fallback chain, and split the resolver`，3 files +171/−22（`src/handoff.ts`、`test/logic.test.mjs`、`CHANGELOG.md`）；History: `e101db1` → `8557fb1` (wrong cap) → `011a68e` (revert) → `a7b1c1d`。`a7b1c1d` 之后另有 `843c2ed`/`0493dcf`（按职责拆模块，旧路径 `src/handoff.ts`、`src/shared/autolearn.ts` 已不存在）、`06c6059`（删测压节流）、`3ed2588`/`8f247b8`/`c8c8d79`（卡片改用平台 `SettingsForm` + 常驻渲染测试）、`b2c9402`（清掉仍指向已删节流的注释）。
- Correct formula: `quality = upstreamUsableInput ?? knee(window)`, then `threshold = min(max(baseline + keep + summarizeAmount, quality), usable − SAFETY_MARGIN)`. Curve verbatim from pi: `round(W − (W − 157K) / (1 + e^(−ln(W/450K)/0.04)))`, constants `KNEE_ASYMPTOTE_TOKENS` 157_000 / `KNEE_TRANSITION_TOKENS` 450_000 / `KNEE_TRANSITION_STEEPNESS` 0.04.
- **dsh exposes no upstream usable-input field.** `LlmResolvedModelInfo.context` is only `{ contextWindow: number }`; `auto_compact_token_limit`, `usableInputTokens` and `maxInputTokens` appear nowhere in dsh. So the chain always takes its knee branch; `qualityLimit(window, upstream?)` is the seam for a harness that separates declared capacity from usable input. The `discovery.ts` `capacity()` merge is dsh **core**, not this repo.
- **Behaviour change, measured** (default target 64000, baseline 6000, keep 20000): 65K 37576→**45152**, 128K 68808→**107616**, 200K 90000→**179616**, 400K 90000→**379616**, 1M/2M 90000→**157000**. Those reproduce pi's documented `boundary` shares 84% / 90% / 95% / 16% / 8%. The `W=40000 / keep=0 / baseline=11800` refusal survives (capacity 19616 < floor 19800), so `e101db1`'s misattribution test still holds.
- **The three receipts fixed in `e101db1` are unchanged**: `/handoff status` names the term that actually refused; `/handoff now` splits deferred / transient / terminal; `/autolearn` reports skipped / rejected. pi's own refusal receipt (`6e620a5`, six `undefined` exits including a billing one) was verified separately at 9/9.
- `resolveThreshold` is now five single-purpose pieces: `handoffRoom` (feasibility only), `qualityLimit` (the fallback chain), `capacityLimit` (the bound), `summarizeAmount` (how much to fold), and an exported orchestrator that only selects a mode and composes them. `thresholdRefusal` reuses `handoffRoom` instead of re-deriving the same terms, so the two can only diverge if the *composition* changes. Six tests had encoded the old shallow trigger and were updated at the fixture level, not weakened.
- Mutations killed, all valid (compile 0, marker in `lib/`, behaviour changes): reversing the chain → 1 check red; defeating the capacity cap → 11 red; making the target inert → 1 red. **The first attempts at the cap and target mutants failed `tsc` (`TS6133`, symbols left unused) — an invalid mutant's red is not evidence.**
- **Lesson worth keeping**: my own memory had recorded the knee direction wrongly, and trusting it over the sessions caused the regression. The user's corrections were several turns deep in `session-8256f99d`; recovering them required parsing `user/message` events with `source.kind == "user"` out of `.agents/memory/session-logs/*/session.jsonl` (runtime-context snapshots and handoff banners are also `user/message` and drown the signal).
- `PI_PKG` trap recurred: the nix glob matched **two** pi versions (0.85.1 and 0.86.1) and `$(ls -d …)` concatenated them into a broken path, so the suite read as `9 of 9 failed` + `ERR_MODULE_NOT_FOUND`. Pass one explicit path; both versions pass 9/9 separately.
- `MEMORY.md` 当前 ~27.2K chars（API 校验 `truncated: false` / `damaged: 0` / `poisoned: false`，余量 ~4.8K —— 已接近 32K 上限，下次追加前先合并旧条目）。最新门禁（`b2c9402`）：typecheck 0, build 0, 225/225, `lib/` mutant markers 0，未跟踪只有 `.agents/`。本轮另清掉记忆里最后一批过期断言：`origin/main` 旧值、`50d5f06` 守卫「零覆盖」、pi 缺 `MAX_SKILL_BODY_CHARS` 的「upstream issue」、以及拆模块前的一批裸 `:NNN` 行号（已改为符号引用）。

## Open tasks

- ~~Propose the `dsh web` restart that loads the host-side changes~~ **已作废（核实后不成立）**：本 GUI 由**桌面端**（electron PID 82600 + host 子进程 node PID 82709，启动 15:36:57）在 `127.0.0.1:19387` 服务，**3080 上没有 `dsh web`**；该 host 启动时间晚于全部功能性 `src/` 提交（之后唯一的 `b2c9402` 只改注释），所以 `a7b1c1d`/`e101db1`/`f2acbae`/`3da2517` **已经生效，无需重启**。客户端改动本来就不需要 host 重启（按请求带 rev 下发），硬刷新标签页即可。若将来确有 host 侧改动要加载，重启对象是**桌面端**，且必须先完全退出（单实例锁会让新实例直接退出）。
- ~~Fix dsh `maybeAutoHandoff` 的 15 s 测压节流~~ **已关闭**：`06c6059` 直接删掉了这个节流（`pressureCheckedAt` 在 `src/` 里已不存在，每个 `turn/end` 都重新测量），R3 那条「三次提前返回吃掉窗口」随之消失。
- ~~去掉 `approveCandidate` 与 `rejectionReason` 的重复准入规则~~ **已关闭**：`2745df4` 让两者共用导出的 `shapeRejection`（`src/project-autolearn/candidate.ts:47`），`test/autolearn.test.mjs:624` 从谓词 / pass / approve 三个角度钉同一原因。
- **Upstream (dsh core, not this repo)**: split `capacity()` in `discovery.ts` so a declared total window and a usable-input limit are distinct fields; only then can `qualityLimit`'s upstream branch be wired.
- R3 audit 的既有两条已关闭（见上）；R1 = full-text scan；R2 = caller-side reverse lookup、test-coverage reverse lookup、upstream-contract dependencies。
- 设置卡片真机渲染已证实（`b2c9402`，证据 `.agents/evidence/2026-09-26-settings-card-render/`）：布尔=平台开关、枚举=胶囊、底部只有平台「保存」；结构断言由 `test/card-render.test.mjs` 覆盖。
- ~~Add the cheap regression guard for `50d5f06`~~ **已落地**：`test/client-inject.test.mjs:34-68` 断言静态 `inject` 恰为 `["slots","locale"]`、`settingsScope` 与退役槽位 `settings.plugin.item` 都不得回归。
- ~~Decide the un-rendered settings card under dsh alpha.1~~ **已消解**：跑的是 0.1.7-rc.2，卡片真机渲染正常（见上），alpha.1 的「卡片不渲染」只剩历史记录价值。
- ~~Decide the fate of `docs/upstream-pi-triage.md`~~ **早已解决（记忆过期）**：它**早就入库了**——`01809c5 docs: close the pi-to-dsh port triage as a record, not a plan`（2026-09-25 17:35），已在 `origin/main`；`git status docs/` 干净。记忆里「still untracked / 未决定」的说法是错的。仍在跟踪的上游 issue 分析继续放 `docs/upstream-issue-client-send-path.md`。
- ~~Decide whether `.agents/evidence/` gets committed~~ **已决定并提交（2026-09-26）**：`.agents/evidence/2026-09-26-settings-card-render/`（7 PNG + README）与 `.agents/memory/MEMORY.md`/`CONTEXT.md` 一并入库；`.agents/memory/session-logs/`、`.agents/skills/` 仍不跟踪。
- Watch `MEMORY.md` against the 32000 cap (now ~27.2K, ~4.8K headroom — merge before appending); edit-tool writes bypass the plugin write path — verify with `loadMemory(root, 32000)` and `isMemoryTruncated(loaded.text) === false` plus `damaged`/`poisoned`.
- Optional upstream-facing: the absence of richer model metadata on `LlmDiscoveredModel`. ~~pi's `/autolearn approve` missing `MAX_SKILL_BODY_CHARS` + `skillBodyUnsafe`~~ **已关闭**（2026-09-26）：pi 也已共用导出谓词 `shapeRejection`（`extensions/project-context/autolearn/candidate.ts:34`，`approveCandidate` 在 `:88` 调用），由 `33e8d9e`/`1aff2aa` 落地。
- `dsh-client-auto-continue` 补丁的版本钉死风险已写进说明：`/etc/nixos/scripts/dsh-plugin-patches/PINNING-RISK.md`（该文件在 /etc/nixos 里**未提交**，等你 review）。要点：补丁是本机自维护、当前 **0.11.8**（不是 0.11.7；客户端半已被上游修掉），真正的残余风险是 profile 依赖写的是范围号 `^0.11.8` 而补丁 key 是精确号 —— 手工 `pnpm add/update` 能漂到 0.11.9 而 pnpm **静默跳过**补丁；唯一自动拦截是 `scripts/check-dsh-plugin-patches.sh`（当前双 profile 全绿）。

<!-- latest-session-title: dsh-project-context — quality knee as a fallback chain, resolver split -->
